import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  navigationBarStyle: {
    backgroundColor: '#2e2e2e',
  },
  tabBarStyle: {
    borderTopWidth: 0.5,
    borderColor: '#b7b7b7',
    backgroundColor: '#fff',
    opacity: 1,
  },
});
